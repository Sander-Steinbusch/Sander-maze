name: Pull Request

on:
  pull_request:

permissions:
  contents: read

jobs:
  python:
    uses: ./.github/workflows/python.yml
    secrets:
      openai-api-key: ${{ secrets.AZURE_OPEN_AI_SERVICE_KEY }}