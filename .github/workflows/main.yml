name: Main

on:
  push:
    branches:
      - main

concurrency:
  group: main

permissions:
  contents: read

jobs:
  python:
    uses: ./.github/workflows/python.yml
    secrets:
      openai-api-key: ${{ secrets.AZURE_OPEN_AI_SERVICE_KEY }}